@import 'Babelhome.css';

:root {
    --paper-color: #c2c0a9;
    --ink-color: #1a1a1a;
    --stamp-color: #a31d1d;
    --bg-dark: #0a0a0a;
    --nav-height: 104px;
    --footer-height: 230px;
    --sidebar-width: 280px;
    --overlay-color: rgba(0, 0, 0, 0.55);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    overflow-x: hidden;
}

body {
    background-color: var(--bg-dark);
    color: var(--ink-color);
    font-family: 'Courier New', Courier, monospace;
    display: flex;
    overflow-x: hidden;
    flex-direction: column;
    min-height: 100vh;
    line-height: 1.45;
}

.scroll-top-btn {
    position: fixed;
    right: 32px;
    bottom: 32px;
    z-index: 12;
    width: 58px;
    height: 58px;
    border-radius: 50%;
    border: 1px solid rgba(255, 255, 255, 0.75);
    background: rgba(0, 0, 0, 0.58);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    opacity: 0;
    pointer-events: none;
    transform: translateY(12px) scale(0.96);
    transition: opacity 0.3s ease, transform 0.3s ease, background 0.3s ease, border-color 0.3s ease;
}

.scroll-top-btn:hover {
    background: rgba(0, 0, 0, 0.82);
    border-color: rgba(255, 255, 255, 1);
    transform: translateY(0) scale(1);
    filter: drop-shadow(0 0 12px rgba(255, 255, 255, 0.45));
}

body.show-top-btn .scroll-top-btn {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0) scale(1);
}

.page-shell {
    flex: 1;
    display: flex;
    width: 100%;
    flex-direction: column;
    min-height: 100vh;
}

.page-hero {
    position: relative;
    padding-bottom: 90px;
}

.page-hero .scroll-btn {
    left: 50%;
    transform: translateX(-50%);
}
nav {
    height: var(--nav-height);
    background-color: var(--paper-color);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 20px;
}
header {
    position: fixed;
    top: 0;
    width: 100%;
    padding: 20px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 10;
    transition: opacity 0.35s ease, transform 0.35s ease;
    will-change: opacity, transform;
}

body.header-hidden header {
    opacity: 0;
    transform: translateY(-24px);
    pointer-events: none;
}

.brand-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: inherit;
    text-decoration: none;
    cursor: pointer;
}

.brand-container img {
    width: 40px;
    display: block;
    margin: 0 auto 5px;
    filter: invert(1);
}

.menu-icon {
    cursor: pointer;
}

.user-icon {
    cursor: pointer;
}

.top-nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: var(--nav-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 38px;
    color: #f4f4f4;
    background: linear-gradient(to bottom, rgba(24, 24, 24, 0.98), rgba(10, 10, 10, 0.94));
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.55);
    z-index: 50;
}

.nav-side,
.menu-icon {
    cursor: pointer;
}

.nav-side i {
    transition: transform 0.2s ease, color 0.2s ease;
}

.nav-side:hover i {
    transform: scale(1.08);
    color: var(--stamp-color);
}
.nav-side {
    width: 42px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.8rem;
    opacity: 0.95;
}
.nav-brand {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-transform: uppercase;
    letter-spacing: 8px;
    gap: 8px;
    transform: translateY(-2px);
    color: inherit;
    text-decoration: none;
    cursor: pointer;
}

.nav-brand i {
    font-size: 1.8rem;
    line-height: 1;
}

.nav-brand span {
    font-size: 1.05rem;
    white-space: nowrap;
}

.sidebar {
    position: fixed;
    top: 0;
    left: calc(-1 * var(--sidebar-width));
    width: var(--sidebar-width);
    height: 100vh;
    color: rgba(235, 235, 235, 0.88);
    background:
        linear-gradient(180deg, rgba(7, 10, 16, 0.78) 0%, rgba(5, 8, 14, 0.7) 100%),
        radial-gradient(circle at 30% -10%, rgba(255, 255, 255, 0.09) 0%, rgba(255, 255, 255, 0) 58%);
    border-right: 1px solid rgba(220, 220, 220, 0.18);
    box-shadow: 14px 0 44px rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(10px) saturate(120%);
    z-index: 110;
    display: flex;
    flex-direction: column;
    transition: left 0.42s cubic-bezier(0.2, 0.7, 0.2, 1), opacity 0.35s ease;
    overflow: hidden;
    opacity: 0.96;
}

.sidebar::before {
    content: "";
    position: absolute;
    inset: auto -18% -12% -18%;
    height: 42%;
    background:
        radial-gradient(ellipse 34% 16% at 18% 88%, rgba(255, 255, 255, 0.08) 0%, transparent 70%),
        radial-gradient(ellipse 36% 18% at 68% 92%, rgba(255, 255, 255, 0.06) 0%, transparent 72%);
    filter: blur(18px);
    pointer-events: none;
}

.sidebar.active {
    left: 0;
}

.sidebar-header {
    padding: 26px 22px;
    border-bottom: 1px solid rgba(220, 220, 220, 0.14);
    display: flex;
    align-items: center;
    gap: 12px;
    color: rgba(245, 245, 245, 0.9);
}

.sidebar-header i {
    font-size: 1.35rem;
    opacity: 0.85;
}

.sidebar-header span {
    font-size: 0.88rem;
    letter-spacing: 4px;
    font-weight: 700;
    text-transform: uppercase;
}

.sidebar-menu {
    list-style: none;
    padding: 16px 0;
    flex: 1;
}

.sidebar-menu li a {
    display: flex;
    align-items: center;
    gap: 11px;
    padding: 13px 24px;
    color: rgba(224, 224, 224, 0.7);
    text-decoration: none;
    font-size: 1.02rem;
    letter-spacing: 0.9px;
    transition: color 0.24s ease, background 0.24s ease, transform 0.24s ease, border-color 0.24s ease;
    border-left: 2px solid transparent;
    text-shadow: 0 0 0 transparent;
}

.sidebar-menu li a i {
    opacity: 0.8;
    font-size: 1rem;
}

.sidebar-menu li a:hover {
    color: rgba(245, 245, 245, 0.96);
    background: rgba(255, 255, 255, 0.05);
    border-left-color: rgba(215, 215, 215, 0.6);
    transform: translateX(3px);
}

.sidebar-menu li a.active {
    color: #f1f1f1;
    background: linear-gradient(90deg, rgba(134, 20, 20, 0.24), rgba(134, 20, 20, 0.08) 62%, rgba(255, 255, 255, 0));
    border-left-color: rgba(145, 30, 30, 0.7);
    text-shadow: 0 0 9px rgba(255, 255, 255, 0.16);
}

.sidebar-footer {
    padding: 18px 22px 20px;
    border-top: 1px solid rgba(220, 220, 220, 0.12);
    background: linear-gradient(180deg, rgba(6, 9, 14, 0.25), rgba(6, 9, 14, 0.45));
}

.soldier-status {
    display: flex;
    flex-direction: column;
    align-items: stretch;
    gap: 10px;
    margin-bottom: 12px;
    color: rgba(233, 233, 233, 0.82);
}

.status-top {
    display: flex;
    align-items: center;
    gap: 8px;
}

.sidebar-footer .status-label {
    font-size: 0.72rem;
    letter-spacing: 2.4px;
    text-transform: uppercase;
    opacity: 0.72;
}

.soldier-status i {
    font-size: 1rem;
    color: rgba(179, 0, 0, 0.82);
}

.lives-bar {
    display: flex;
    gap: 8px;
    flex-wrap: nowrap;
}

.life {
    width: 24px;
    height: 7px;
    border-radius: 2px;
    background: rgba(235, 235, 235, 0.11);
    border: 1px solid rgba(255, 255, 255, 0.12);
    transition: background 0.25s ease, box-shadow 0.25s ease;
}

.life.active {
    background: linear-gradient(90deg, rgba(140, 10, 10, 0.92), rgba(179, 25, 25, 0.95));
    box-shadow: 0 0 10px rgba(179, 25, 25, 0.35);
    border-color: rgba(165, 35, 35, 0.4);
}

.rank-display {
    display: flex;
    align-items: center;
    gap: 10px;
    padding-top: 10px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    color: rgba(225, 225, 225, 0.78);
    letter-spacing: 1px;
}

.rank-icon {
    font-size: 1rem;
    opacity: 0.84;
}

.rank-name {
    font-size: 0.93rem;
}

.sidebar-overlay {
    position: fixed;
    inset: 0;
    background: radial-gradient(circle at 20% 20%, rgba(18, 24, 36, 0.24), rgba(0, 0, 0, 0.54));
    backdrop-filter: blur(3px);
    opacity: 0;
    pointer-events: none;
    z-index: 105;
    transition: opacity 0.34s ease;
}

.sidebar-overlay.active {
    opacity: 1;
    pointer-events: auto;
}



/* ============================================
   EFECTOS GLOBALES (Spotlight + Niebla)
   ============================================ */

.spotlight {
    position: fixed;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100%;
    height: 100vh;
    background: radial-gradient(circle at top center, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
    z-index: 2;
    pointer-events: none;
}

.fog-container {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 66vh;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
    filter: blur(24px);
    opacity: 0.98;
    mix-blend-mode: screen;
    animation: fog-float 16s ease-in-out infinite;
}

.fog-container::before,
.fog-container::after {
    content: "";
    position: absolute;
    inset: -22% -18% -8% -18%;
    background:
        radial-gradient(ellipse 20% 10% at 14% 88%, rgba(255, 255, 255, 0.34) 0%, rgba(255, 255, 255, 0.18) 40%, transparent 72%),
        radial-gradient(ellipse 22% 12% at 30% 82%, rgba(255, 255, 255, 0.3) 0%, rgba(255, 255, 255, 0.12) 42%, transparent 74%),
        radial-gradient(ellipse 28% 14% at 50% 90%, rgba(255, 255, 255, 0.26) 0%, rgba(255, 255, 255, 0.1) 38%, transparent 76%),
        radial-gradient(ellipse 24% 12% at 72% 84%, rgba(255, 255, 255, 0.28) 0%, rgba(255, 255, 255, 0.12) 40%, transparent 74%),
        radial-gradient(ellipse 18% 10% at 88% 92%, rgba(255, 255, 255, 0.22) 0%, rgba(255, 255, 255, 0.08) 40%, transparent 76%),
        linear-gradient(to top, rgba(255, 255, 255, 0.2) 0%, rgba(255, 255, 255, 0.08) 35%, transparent 78%);
    transform: translate3d(0, 0, 0);
    background-size: 180% 180%;
    background-repeat: no-repeat;
}

.fog-container::before {
    animation: fog-drift-left 26s ease-in-out infinite, fog-curl-left 10s ease-in-out infinite;
}

.fog-container::after {
    opacity: 0.82;
    animation: fog-drift-right 34s ease-in-out infinite, fog-curl-right 13s ease-in-out infinite;
}

body.footer-mode .fog-container {
    opacity: 0.32;
    transform: translateY(8px);
    transition: opacity 0.6s ease, transform 0.6s ease;
    pointer-events: none;
}

@keyframes fog-float {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-3vh) scale(1.02); }
}

@keyframes fog-drift-left {
    0% { transform: translateX(-10%) translateY(0) scale(1.02); }
    35% { transform: translateX(2%) translateY(-1.2%) scale(1.05); }
    70% { transform: translateX(7%) translateY(-2.2%) scale(1.07); }
    100% { transform: translateX(-10%) translateY(0) scale(1.02); }
}


@keyframes fog-drift-right {
    0% { transform: translateX(10%) translateY(0) scale(1.04); }
    30% { transform: translateX(1%) translateY(-1%) scale(1.06); }
    65% { transform: translateX(-7%) translateY(-2%) scale(1.09); }
    100% { transform: translateX(10%) translateY(0) scale(1.04); }
}

@keyframes fog-curl-left {
    0%, 100% { background-position: 0% 100%; opacity: 0.66; }
    25% { background-position: 12% 92%; opacity: 0.9; }
    50% { background-position: 24% 82%; opacity: 0.78; }
    75% { background-position: 10% 88%; opacity: 0.94; }
}

@keyframes fog-curl-right {
    0%, 100% { background-position: 100% 85%; opacity: 0.52; }
    30% { background-position: 82% 96%; opacity: 0.82; }
    60% { background-position: 70% 84%; opacity: 0.68; }
    85% { background-position: 90% 92%; opacity: 0.88; }
}


/* ============================================
   CONTENIDO PRINCIPAL
   ============================================ */

main {
    flex: 1;
     position: relative;
      z-index: 5;
       padding-top: calc(var(--nav-height) + 30px);
    padding-bottom: 30px;

   
    background-color: var(--paper-color);
}

.paper-container {
    position: relative;
    width: 90%;
    max-width: 820px;
    background: linear-gradient(180deg, #cbc7b4 0%, #c2c0a9 100%);
    background-image: url('https://www.transparenttextures.com/patterns/paper-fibers.png');
    padding: 44px;
    box-shadow: 0 18px 60px rgba(0, 0, 0, 0.55), inset 0 0 90px rgba(0, 0, 0, 0.08);
    border-radius: 6px;
    border: 1px solid rgba(26, 26, 26, 0.18);
    transform: rotate(-0.35deg);
    margin: 0 auto;
    font-family: 'Courier New', Courier, monospace;
}

h1 {
    text-align: center;
    text-transform: uppercase;
    font-size: clamp(1.4rem, 2vw, 2rem);
    border-bottom: 2px solid var(--ink-color);
    padding-bottom: 10px;
    margin-bottom: 30px;
    letter-spacing: 2px;
    color: var(--ink-color);
    font-weight: 700;
}

.field {
    margin-bottom: 22px;
    border-bottom: 2px solid var(--ink-color);
    display: flex;
    align-items: baseline;
    gap: 14px;
    padding-bottom: 8px;
    transition: border-color 0.2s ease, transform 0.2s ease;

}

.field:focus-within {
    border-bottom-color: var(--stamp-color);
    transform: translateX(2px);
}

.field-with-status {
    align-items: center;
}

.date-field {
    align-items: center;
}


.input-wrap {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 10px;
    min-width: 0;
}

.field-status {
    width: 22px;
    height: 22px;
    flex: 0 0 22px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: transparent;
    opacity: 0;
    transform: scale(0.85);
    transition: opacity 0.18s ease, transform 0.18s ease, color 0.18s ease;

}

.field-status i {
    font-size: 1rem;
}



.field.invalid .field-status {
    color: var(--stamp-color);
    opacity: 1;
    transform: scale(1);

}


.field.valid .field-status {
    opacity: 0;

}


label {
    font-size: 1.05rem;
    color: var(--ink-color);
    white-space: nowrap;
    font-weight: 700;
    letter-spacing: 0.6px;

}




select {
    background: transparent;
    border: none;
    flex: 1;
    width: 100%;
    min-width: 0;
    font-family: 'Courier New', Courier, monospace;
    font-size: 1.12rem;
    line-height: 1.45;
    color: var(--ink-color);
    outline: none;
    font-weight: 700;
    text-shadow: 1px 1px 0px rgba(0, 0, 0, 0.72), -0.5px -0.5px 0px rgba(0, 0, 0, 0.35);
    filter: blur(0.15px) contrast(140%);
    letter-spacing: 1px;
    padding: 4px 0 8px;
}


input::placeholder {
    color: rgba(26, 26, 26, 0.48);
}

input,
select {
    width: 100%;
    min-width: 0;
    border-radius: 0;
}

input {
    background: transparent;
    border: none;
    color: var(--ink-color);
    font-family: 'Courier New', Courier, monospace;
    font-size: 1.1rem;
    line-height: 1.45;
    outline: none;
    font-weight: 700;
    letter-spacing: 0.6px;
    padding: 5px 0 8px;
}

input:focus,
select:focus {
    text-shadow: 0 0 0 transparent;
}


select {
    appearance: none;
    cursor: pointer;

}


select::-ms-expand {
    display: none;
}


.select-wrap {
    flex: 1;
    position: relative;
    min-width: 0;
}


.select-wrap::after {
    content: "▾";
    position: absolute;
    right: 4px;
    top: 50%;
    transform: translateY(-50%);
    color: rgba(26, 26, 26, 0.9);
    pointer-events: none;
    font-size: 1rem;
}


select option {
    color: #1a1a1a;
    background: #e6e2cf;

}

.btn-enviar {
    display: block;
    margin: 40px auto 0;
    background: linear-gradient(180deg, rgba(26, 26, 26, 0.08), rgba(26, 26, 26, 0.02));
    border: 2px solid var(--ink-color);
    padding: 12px 30px;
    font-family: 'Courier New', Courier, monospace;
    font-size: 1.08rem;
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.2s ease, background 0.2s ease, color 0.2s ease, box-shadow 0.2s ease;
    text-transform: uppercase;
    color: var(--ink-color);
    box-shadow: 0 8px 18px rgba(0, 0, 0, 0.12);

}

.btn-enviar:hover {
    background: var(--ink-color);
    color: var(--paper-color);
    transform: translateY(-1px);
    box-shadow: 0 12px 22px rgba(0, 0, 0, 0.18);
}

.summary-view {
    margin-top: 26px;
    padding-top: 18px;
    border-top: 2px dashed rgba(26, 26, 26, 0.35);
}

.summary-field {
    display: flex;
    justify-content: space-between;
    gap: 18px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(26, 26, 26, 0.18);
}

.summary-field label {
    font-size: 1rem;
}

.summary-field span {
    text-align: right;
    font-weight: 700;
    color: var(--ink-color);
}

.hidden {
    display: none !important;
}

.blackout {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.92);
    color: #fff;
    display: none;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    text-align: center;
    z-index: 120;
    gap: 8px;
}

.blackout h2 {
    color: var(--stamp-color);
    letter-spacing: 2px;
}

.blackout p {
    color: rgba(255, 255, 255, 0.78);
}


/* ============================================
   SELLO Y BLACKOUT - CORREGIDO
   ============================================ */

#stamp {
    position: fixed;           /* Fuera del flujo, no empuja nada */
    top: 50%;                   /* Centrado en pantalla */
    left: 50%;
    transform: translate(-50%, -50%) scale(0.8);  /* Centrado + un poco pequeño */
    border: 6px solid var(--stamp-color);
    color: var(--stamp-color);
    padding: 15px 30px;
    font-size: 3rem;
    font-weight: bold;
    text-transform: uppercase;
    border-radius: 10px;
    opacity: 0;
    visibility: hidden;        /* No ocupa espacio */
    pointer-events: none;
    mix-blend-mode: multiply;
    filter: blur(0.3px);
    z-index: 130;
    transition: opacity 0.5s ease, transform 0.5s ease, visibility 0.5s;
}

#stamp.stamp-active {
    opacity: 0.85;
    visibility: visible;
    transform: translate(-50%, -50%) scale(1);  /* Tamaño normal */
}
/* ============================================

   FOOTER - FIJO ABAJO CON NIEBLA DENTRO

   ============================================ */


footer {
    position: relative;
    overflow: hidden;
    background: linear-gradient(to top, #000 0%, #020202 55%, #070707 100%);
    padding: 70px 50px 55px;
    font-size: 0.8rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    color: #a0a0a0;
    text-align: center;
    z-index: 10;
    margin-top: auto;
}

footer::before {
    top: -70px;
    height: 140px;
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    background: linear-gradient(to bottom, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.03) 35%, transparent 100%);
    filter: blur(18px);
    opacity: 0.7;
    animation: footer-fog-rise 14s ease-in-out infinite;
}


footer::after {
    inset: 0;
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    background:
        radial-gradient(ellipse 22% 10% at 14% 88%, rgba(255, 255, 255, 0.06) 0%, transparent 72%),
        radial-gradient(ellipse 24% 12% at 40% 92%, rgba(255, 255, 255, 0.045) 0%, transparent 74%),
        radial-gradient(ellipse 26% 12% at 68% 86%, rgba(255, 255, 255, 0.05) 0%, transparent 76%),
        radial-gradient(ellipse 18% 10% at 88% 90%, rgba(255, 255, 255, 0.04) 0%, transparent 78%);
    filter: blur(14px);
    opacity: 0.42;
    animation: footer-fog-drift 18s ease-in-out infinite;
}


footer > * {
    position: relative;
    z-index: 1;
}


@keyframes footer-fog-rise {
    0%, 100% { transform: translateY(0); opacity: 0.5; }
    50% { transform: translateY(-10px); opacity: 0.9; }

}


@keyframes footer-fog-drift {
    0%, 100% { transform: translateX(-1%) translateY(0); }
    50% { transform: translateX(1.5%) translateY(-4px); }

}


.footer-line {
    margin: 5px 0;
}


.highlight {
    color: #d0d0d0;
}

@media (max-width: 900px) {
    main {
        padding-top: calc(var(--nav-height) + 20px);
    }

    .paper-container {
        width: min(94%, 820px);
        padding: 34px 22px;
    }

    .field,
    .summary-field {
        flex-direction: column;
        align-items: stretch;
        gap: 8px;
    }

    .summary-field span {
        text-align: left;
    }

    .nav-brand span {
        letter-spacing: 4px;
        font-size: 0.88rem;
    }
}

@media (max-width: 640px) {
    .top-nav {
        padding: 0 18px;
    }

    .nav-brand {
        letter-spacing: 4px;
        gap: 4px;
    }

    .paper-container {
        width: 96%;
        padding: 26px 18px;
        transform: none;
    }

    h1 {
        margin-bottom: 22px;
    }

    label,
    input,
    select {
        font-size: 1rem;
    }

    .btn-enviar {
        width: 100%;
    }
}






